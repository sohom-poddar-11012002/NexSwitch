suite:
  id: v1-full-suite
  name: "v1 full regression suite"
  description: "All Phase 1+2 runs — golden path, boundary, security. Run before every deployment."
  mode: SEQUENTIAL
  parallelism: 1

  run_ids:
    - golden-path-run
    - boundary-run
    - security-run
