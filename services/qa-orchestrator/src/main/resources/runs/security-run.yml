run:
  id: security-run
  name: "Security adversarial run"
  description: "Replay attack, oversized field injection. STATELESS — adversarial scenarios must not share state."

  session:
    mode: STATELESS
    isolate_on_failure: false

  variables:
    terminal_id: "TERM0042"
    merchant_id: "MERCH0000999"
    rest_base_url: "http://acquiring-service:8080"
    rest_api_key: "dev-api-key-change-me"

  scenarios:
    - id: replay-attack
    - id: oversized-field
    - id: reset-atc-watermarks
    - id: emv-chip-arqc-invalid
