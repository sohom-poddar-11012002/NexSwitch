run:
  id: boundary-run
  name: "Boundary — rejection cases"
  description: "Invalid Luhn, zero amount, unknown BIN, wrong currency, max amount. STATELESS — each scenario isolated."

  session:
    mode: STATELESS
    isolate_on_failure: false

  variables:
    terminal_id: "TERM0042"
    merchant_id: "MERCH0000999"

  scenarios:
    - id: invalid-luhn
    - id: zero-amount
    - id: unknown-bin
    - id: wrong-currency
    - id: max-amount
